/**
 * Example Astro configuration with astro-auto-load
 *
 * This is all you need! The middleware is automatically injected.
 */
import { defineConfig } from 'astro/config';
import autoLoad from 'astro-auto-load';

// https://astro.build/config
export default defineConfig({
  output: 'server', // or 'hybrid' - auto-load requires SSR
  integrations: [
    autoLoad(),
    // ... other integrations
  ],
});

