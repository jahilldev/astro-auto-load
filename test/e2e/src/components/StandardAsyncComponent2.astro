---
// Traditional async approach - no loader, just async/await in frontmatter
const start = Date.now();
await new Promise((resolve) => setTimeout(resolve, 50));
const duration = Date.now() - start;
---

<div class="standard-async-component-2" data-start={start} data-duration={duration}>
  <h4>StandardAsyncComponent2 (async: {duration}ms)</h4>
  <slot />
</div>
