---
import { getLoaderData } from 'astro-auto-load/runtime';

export const loader = async () => {
  const start = Date.now();
  await new Promise((resolve) => setTimeout(resolve, 50));
  const duration = Date.now() - start;
  return { start, duration, component: 'AsyncComponent3' };
};

const data = await getLoaderData<typeof loader>();
---

<div class="async-component-3" data-start={data.start} data-duration={data.duration}>
  <h5>AsyncComponent3 (loader: {data.duration}ms)</h5>
</div>
